name: ota_protocol
description: Shorebird-compatible OTA check/events protocol types for Meta Code Push
version: 0.1.0
environment:
  sdk: ^3.5.0
dependencies:
  json_annotation: ^4.9.0
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
