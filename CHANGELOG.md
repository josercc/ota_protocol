## 0.1.1

- `PatchArtifact` carries optional `resource_number`, `config_fingerprint`, and
  `has_resource_changes` from `/api/v1/patches/check` for per-patch resource OTA.

## 0.1.0

- Initial release: Shorebird-compatible OTA check/events protocol types.
